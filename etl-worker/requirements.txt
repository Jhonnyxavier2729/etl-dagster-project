# Procesamiento de datos (MANTENER tus versions)
pandas==2.1.4
numpy==1.24.3
pyarrow==14.0.2

# Bases de datos (MANTENER tus versions)
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
pymongo==4.6.2
duckdb==0.9.2

# Utilidades (MANTENER tus versions)
python-dotenv==1.0.1
loguru==0.7.2
typing-extensions==4.9.0
pendulum==2.1.2

# NOTA: Las dependencias de Dagster NO van aquí
# Se instalan directamente en el Dockerfile
